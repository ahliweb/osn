---
"osn-informatika-2026": minor
---

Add architecture documentation under `docs/architecture/`: a system
overview with the strict one-way layering rule
(`data` → `schema` → `domain` → `render`/`cli`), a repository map, and five
Architecture Decision Records covering curriculum-as-code, the
Bun/TypeScript/Zod stack, JSON data with Zod contracts, the no-learner-data
rule, and dual versioning.
