---
"osn-informatika-2026": minor
---

Add the seven §6.3 mentor KPI definitions and a pure computation engine
over learning records. Every metric returns a discriminated result that
makes "not enough data" explicit rather than emitting `NaN`, and the
metrics carry §14.2 rule 7's caveat that problem volume is not mastery.
