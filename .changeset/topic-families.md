---
"osn-informatika-2026": minor
---

Add the schema foundation (`src/schema/common.ts`) and the ten official
topic families from §2.1 as validated data, with a typed domain loader
exposing lookup helpers. Establishes the schema → data → domain pattern
that the rest of the curriculum corpus follows.
